#!/bin/sh
ROM=`df -h | grep -w / | tr -s \  | cut -d \  -f 5 | cut -c 1-2`
if [ $ROM -gt 84 ]; then echo '<fc=#FF4500>'$ROM'%</fc>'
elif [ $ROM -gt 59 ]; then echo '<fc=#DFD700>'$ROM'%</fc>'
else echo $ROM'%'
fi
exit 0