#!/usr/bin/env zsh
# Author - Haron Prime
# License © 2017 WTFPL http://www.wtfpl.net/

ST=`mpc | grep "\[*\]" | cut -d \  -f 1 | sed -e 's/[^a-z]//g'`
case $ST in
    playing  ) echo "<fn=3></fn>" ;;
    paused   ) echo "<fn=3></fn>" ;;
    *        ) echo "<fn=3></fn>" ;;
esac
exit 0
