#!/usr/bin/env zsh
# Author - Haron Prime
# License © 2017 WTFPL http://www.wtfpl.net/

PID=`ps aux | grep XMTimeNew | grep -v grep | awk '{print $2}'`
if [[ $PID -gt 0 ]]; then
    kill $PID
    echo > /tmp/haron/TIME
    exit 0
else
    XMTimeNew &
    exit 0
fi
